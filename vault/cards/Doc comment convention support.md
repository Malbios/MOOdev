MOOcode has no comment syntax (confirmed in the M3 notes), but the codebase already leans on a bare leading string-literal as an informal doc line (moo-vcs-plan.md's own verb sketch does this: "Describe this room to the caller.";). Hover-over-a-call and outline view could surface that line as documentation.

Done. Hovering a resolved verb call now shows, alongside the existing dispatch metadata: an auto-inferred summary computed purely from the callee's AST (parameters recovered from `{who, ?what = default} = args;` or `name = args[N];`, the properties/verbs/builtins it references, whether it can suspend/fork, whether it may return a value), plus the callee's leading bare string-literal statement (if any) shown as a supplementary "Comment" line - confirmed live that this exact idiom is the only thing that survives a `set_verb_code()`/`verb_code()` round trip; a real `/* ... */` block comment does not; MOO's `verb_code()` unparses from the compiled program, and comments are discarded before compilation, never reaching it.