The dead-code sweep found something that isn't quite "dead code" — a near-complete verb-history/diff/restore feature (Sidecar's verbHistory/verbAtCommit + resolveVerbPath, Program.fs's "verb-history"/"verb-at-commit" dispatch, and a fully-built client pane in App.fs/index.html: history list, diff editor, Restore button, response handlers). Everything works except one missing piece: nothing ever sets showingVerbHistory to true or fires the initial fetch, so the pane can never actually open. This matches the VCS plan's Phase 5 "per-verb timeline"/"diff view"/"restore" bullets, so it looks like unfinished work rather than an abandoned decision like M5. What should happen to it?